apiVersion: v1
kind: Service
metadata:
  name: authentication
  labels:
    name: authentication
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    name: authentication