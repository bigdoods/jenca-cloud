apiVersion: v1
kind: Service
metadata:
  name: authentication-storage
  labels:
    name: authentication-storage
spec:
  type: LoadBalancer
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    name: authentication-storage